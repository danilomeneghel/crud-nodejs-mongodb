<% layout('base') -%>
<div class="panel">
    <div class="panel-default panel-home">
        <h2>Welcome, <% if(user) { %><span class="name"><%= user.name %></span><% } %>!</h2>
        <span>Delivery Manager</span>
    </div>
</div>